.tags {
  display: flex;
  flex-wrap: wrap;
  margin-bottom: 35px;
  &__item {
    padding: 14px 22px;
    font-size: 15px;
    background: #F3F6F9;
    border-radius: 40px;
    & + & {
      margin-left: 8px;
    }
  }
}