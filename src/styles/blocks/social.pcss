.socials {
  display: flex;

  &__item {
    margin-right: 35px;
    &:last-child {
      margin-right: 0;
    }
  }

  &__icon {
    width: 25px;
    height: 25px;
    fill:  rgba(255, 255, 255, 0.3);
    transition: 0.3s;
    &:hover {
      fill: rgba(255, 255, 255, 1);
    }
  }
}